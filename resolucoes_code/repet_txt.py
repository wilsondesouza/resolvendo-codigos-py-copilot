# Vamos solicitar uma string e um número inteiro, armazená-las em variáveis, e exibi-las no console repetindo a string o número de vezes especificado

texto = input("Digite uma string: ")
vezes = int(input("Digite um número inteiro: "))

resultado = texto * vezes
print(resultado)